This application saves users into an H2 database. Once the users are registered it grants them access. The dates and times for each access are saved in the database as well. The tab summary displays all data saved.

To access the application go to http://localhost:8080/web/login.html

This application is build with Maven and uses Spring boot. The data is saved into an H2 in-memory SQL database.